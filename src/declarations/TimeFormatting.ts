const enum TimeFormatting
// Eslint does not understand const enum and interprets it as a variable declaration in constant context.
/* eslint-disable-next-line */
{
	DEFAULT_PAD_LENGTH = 2
}

export { TimeFormatting };
